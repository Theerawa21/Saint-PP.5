export function schoolDate(value){if(!value)return null;if(typeof value!=='string')return null;let y,m,d;const iso=/^(\d{4})-(\d{2})-(\d{2})$/.exec(value.trim()),thai=/^(\d{1,2})\/(\d{1,2})\/(\d{4})$/.exec(value.trim());if(iso)[y,m,d]=iso.slice(1).map(Number);else if(thai)[d,m,y]=thai.slice(1).map(Number);else return null;if(y>2400)y-=543;if(y<1900||y>2200)return null;const date=new Date(y,m-1,d);return date.getFullYear()===y&&date.getMonth()===m-1&&date.getDate()===d?date:null;}
export function calendarError(sc){for(const [start,end,label] of [['term1Start','term1End','ภาคเรียนที่ 1'],['term2Start','term2End','ภาคเรียนที่ 2']]){if(sc[start]&&!schoolDate(sc[start])||sc[end]&&!schoolDate(sc[end]))return 'วันที่ของ'+label+'ไม่ถูกต้อง ใช้ วว/ดด/พ.ศ. หรือ ค.ศ.-ดด-วว';if(!!sc[start]!==!!sc[end])return 'กรอกทั้งวันเปิดและวันปิด'+label;if(sc[start]&&schoolDate(sc[start])>schoolDate(sc[end]))return 'วันปิด'+label+'ต้องไม่ก่อนวันเปิด';}if(sc.term1End&&sc.term2Start&&schoolDate(sc.term2Start)<=schoolDate(sc.term1End))return 'ช่วงภาคเรียนที่ 1 และ 2 ต้องไม่ทับกัน';return '';}
